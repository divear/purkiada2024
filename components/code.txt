function calculateProduct(numbers {
    let product = 1;
    for (let num of numbers {
        product *= num;
    }
    return product;
}

function calculateProduct(numbers {\n    let product = 1;\n    for (let num of numbers {\n        product *= num;\n    }\n    return product;\n}